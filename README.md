# bulk-query
LLMs can take large input in a message, but often doesn't return the same granularity of information in their output. Use bulk-query to let them split the content and answer them one after another.
